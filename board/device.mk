# OTA assert
TARGET_OTA_ASSERT_DEVICE := LAVA_R1,Lava,LAVA
# Bionic

# Enable Minikin text layout engine (will be the default soon)
USE_MINIKIN := true

# Configure jemalloc for low memory
MALLOC_SVELTE := true
